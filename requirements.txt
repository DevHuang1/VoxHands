# VoxHands core runs on the Python standard library only, and every optional
# backend is auto-discovered at runtime, so the code needs none of this. The
# include below installs the OpenVINO runtime for the reference deployment so
# the live demo can measure real compiled-model CPU inference on the host
# serving requests. Remove the include to deploy the stdlib-only build.
-r requirements-render.txt
