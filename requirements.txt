# VoxHands core runs on the Python standard library only; this file stays
# intentionally minimal so Render builds cleanly. The optional MuJoCo physics
# backend, OpenVINO vision adapter, and Pillow JPEG encoding are listed in
# requirements-optional.txt and auto-discovered at runtime.